frontend ssh
 bind *:{{ ssh_node_port }}
 default_backend ssh
 mode tcp
 option tcplog
backend ssh
 balance source
 mode tcp
{% for name, ip in workers %}
 server {{ name }} {{ ip }}:{{ ssh_node_port }} check
{% endfor %}

