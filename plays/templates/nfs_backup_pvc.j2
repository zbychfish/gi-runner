kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: backup-nfs-pvc
  namespace: gi
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ gi_backup_volume_size }}Gi
  storageClassName: nfs-client

